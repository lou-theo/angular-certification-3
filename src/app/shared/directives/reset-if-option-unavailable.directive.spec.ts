import { ResetIfOptionUnavailableDirective } from './reset-if-option-unavailable.directive';

describe('ResetIfOptionUnavailableDirective', () => {
  it('should create an instance', () => {
    const directive = new ResetIfOptionUnavailableDirective();
    expect(directive).toBeTruthy();
  });
});
