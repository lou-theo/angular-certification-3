import { ShapeFilterPipe } from './shape-filter.pipe';

describe('ShapeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ShapeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
